name: Test with invalid CloudFormation template
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/main
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          github_token: ${{ github.token }}
          reporter: local
          filter_mode: nofilter
          fail_level: none
          working_directory: "."
          cfn_lint_args: "tests/fixtures/invalid-template.yaml"
      - name: Assert action completed without crashing
        run: |
          echo "Action completed - cfn-lint ran on invalid template and reported issues via reviewdog"
          echo "PASS"
