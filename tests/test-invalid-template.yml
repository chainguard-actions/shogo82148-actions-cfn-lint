name: Test invalid CloudFormation template - lint issues detected
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up git origin reachable inside Docker container
        run: |
          git config user.email "test@example.com"
          git config user.name "Test User"
          rm -rf "$GITHUB_WORKSPACE/.git-remote"
          git init --bare "$GITHUB_WORKSPACE/.git-remote"
          git push "$GITHUB_WORKSPACE/.git-remote" HEAD:refs/heads/act-harness
          git remote set-url origin "$GITHUB_WORKSPACE/.git-remote" 2>/dev/null || \
            git remote add origin "$GITHUB_WORKSPACE/.git-remote"

      - id: action
        uses: .
        continue-on-error: true
        with:
          github_token: ${{ github.token }}
          reporter: local
          filter_mode: nofilter
          fail_level: warning
          cfn_lint_args: tests/fixtures/cfn/invalid-template.yaml

      - name: Assert action detected lint issues or ran successfully
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          # cfn-lint should find issues in the invalid template (failure expected)
          # or it may succeed if reviewdog swallows the exit code
          if [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action correctly detected lint issues in invalid template"
          elif [ "$OUTCOME" = "success" ]; then
            echo "PASS: action ran successfully (reviewdog reported issues locally)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
